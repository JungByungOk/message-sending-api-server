# Dev profile overrides

spring:
  config:
    import: optional:/svc/ems/config/ems-config-dev.yml
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/esm}
    username: ${DB_USERNAME:esm}
    password: ${DB_PASSWORD:esm}
  quartz:
    jdbc:
      initialize-schema: always
    properties:
      org.quartz.dataSource.quartzDataSource.URL: ${QUARTZ_DB_URL:jdbc:postgresql://localhost:5432/esm}
      org.quartz.dataSource.quartzDataSource.user: ${QUARTZ_DB_USER:esm}
      org.quartz.dataSource.quartzDataSource.password: ${QUARTZ_DB_PASSWORD:esm}

aws:
  endpoint: ${AWS_ENDPOINT:http://localhost:4566}
  ses:
    access-key: ${AWS_ACCESS_KEY:test}
    secret-key: ${AWS_SECRET_KEY:test}

security:
  api-key: ${API_KEY:}

management:
  endpoints:
    web:
      exposure:
        include: health,info,quartz
  endpoint:
    health:
      show-details: always
