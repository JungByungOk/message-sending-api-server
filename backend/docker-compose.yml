version: '3.8'

services:
  ems:
    build: .
    container_name: ems-api
    ports:
      - "7092:7092"
    environment:
      - SPRING_PROFILES_ACTIVE=local
      # Database
      - DB_URL=jdbc:postgresql://postgres:5432/esm
      - DB_USERNAME=esm
      - DB_PASSWORD=esm
      - QUARTZ_DB_URL=jdbc:postgresql://postgres:5432/esm
      - QUARTZ_DB_USER=esm
      - QUARTZ_DB_PASSWORD=esm
      # AWS
      - AWS_ACCESS_KEY=test
      - AWS_SECRET_KEY=test
      - AWS_ENDPOINT=http://localstack:4566
      # Security
      - API_KEY=${API_KEY}
    depends_on:
      postgres:
        condition: service_healthy
      localstack:
        condition: service_healthy
    restart: unless-stopped

  localstack:
    image: localstack/localstack:4.4
    container_name: ems-localstack
    ports:
      - "4566:4566"
    environment:
      - SERVICES=ses,dynamodb,sqs,sns
      - DEFAULT_REGION=ap-northeast-2
      - LOCALSTACK_ACKNOWLEDGE_ACCOUNT_REQUIREMENT=1
      - DOCKER_HOST=unix:///var/run/docker.sock
    volumes:
      - localstack-data:/var/lib/localstack
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4566/_localstack/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:17
    container_name: ems-postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=esm
      - POSTGRES_USER=esm
      - POSTGRES_PASSWORD=esm
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U esm -d esm"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
  localstack-data:
