<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <!-- use Spring default values -->
    <include resource="logback/logback-defaults.xml"/>

    <!-- LOGGER -->
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.mybatis" level="INFO"/>
    <logger name="org.mariadb.jdbc" level="INFO"/>
    <logger name="org.quartz" level="DEBUG"/>
    <logger name="com.msas" level="DEBUG"/>

    <!-- APPENDER -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>${CONSOLE_LOG_CHARSET}</charset>
        </encoder>
    </appender>

    <!-- TELEGRAM APPENDER -->
    <appender name="TELEGRAM" class="org.nftreally.TelegramAppender">
        <botToken>5710353844:AAEqVlaqAa7-Ixv-hnximDy8xa_BkkepRV8</botToken>
        <chatId>-1001867481964</chatId>

        <Layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>[%d{HH:mm:ss.SSS}] [%-5level] [%logger{%-36.36t}] - %msg%n</Pattern>
        </Layout>

        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
    </appender>

    <!-- LOGGING -->
    <root level="ERROR">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="TELEGRAM"/>
    </root>
</configuration>